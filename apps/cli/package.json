{
  "name": "@linkedin-scraper/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --target=bun --packages=external --outdir dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@linkedin-scraper/core": "workspace:*",
    "effect": "4.0.0-rc.113"
  }
}
